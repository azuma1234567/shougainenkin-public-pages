# Claude Codeに渡す順番とコマンド — 道具5つ (2026-09-02)

repo: `shougainenkin-public-pages`。**1つずつ、別のセッションで**渡してください。指示書が大きいので、まとめて渡すと精度が落ちます。

---

## 0. 最初に1回だけ(器を作る)

```
docs/feature-gap-analysis-2026-09-02.md の §6 を読んで、道具の置き場所 /dougu を作って。

- /dougu … 道具の一覧ページ。カードを5枚(等級の目安 / 金額 / 必要書類 / 年金事務所 / 申立書)。まだ無いものは「準備中」でリンクしない
- 既存47記事のURL・slug・h1・本文は変更しない。ナビゲーションには /dougu をまだ追加しない
- 既に /tsukuru/ 配下に何か作っていたら /dougu/ へ移し、/tsukuru/* から301リダイレクトを置く

デザイントークンは docs/site-mock-2026-09-02-tools/*.html から取ること。
```

---

## 1. 金額計算機(いちばん軽い。ここから)

```
docs/kingaku-tool-design-2026-09-02.md と docs/site-mock-2026-09-02-tools/Kingaku.html を読んで実装して。
モックは見た目とロジックの正。計算式(§4)は1行も変えないこと。とくに300月みなしと、3級には障害基礎年金が無いことを落とさない。
金額は必ず data/amounts.ts 経由。ハードコード禁止。
入力内容はサーバーへ送らない。§9の完了条件を1項目ずつ○×で報告して。
```

## 2. 等級の目安

```
docs/mitate-tool-design-2026-09-02.md と docs/site-mock-2026-09-02-tools/Mitate.html を読んで実装して。
実装前に §3(絶対条件)と §5(目安表)を必ず読むこと。目安表は1セルでも間違えたら道具として無価値になるので、§5-3 の座標照合を必ず実行して結果を報告すること。
このツールは「判定」をしない。国が公表している表に当てはめて見せるだけ。主語は常に「国のガイドラインでは」。
入力内容と結果は絶対にサーバーへ送らない。§13の完了条件を1項目ずつ○×で報告して。
```

## 3. 必要書類チェックリスト

```
docs/shorui-tool-design-2026-09-02.md と docs/site-mock-2026-09-02-tools/Shorui.html を読んで実装して。
モックは見た目とロジックの正。分岐ルール(§4)と書類データ(モック内 DOCS)をそのまま使うこと。
ただし §6 の「様式番号と正式名称の照合」は必ず実行し、結果を報告すること。文書料の金額は書かないこと。
入力はサーバーへ送らない。§8の完了条件を1項目ずつ○×で報告して。
```

## 4. 申立書をつくる(いちばん重い)

```
docs/moushitatesho-tool-design-2026-09-02.md と docs/site-mock-2026-09-02-tools/Moushitatesho.html を読んで実装して。
実装前に §3(絶対条件)と §10(様式の寸法測定)を先に読むこと。§10 を飛ばして目分量でレイアウトを作らないこと。
URLは /dougu/moushitatesho(設計書の §2 は /tsukuru/ になっているが /dougu/ に読み替える)。
入力内容は絶対にサーバーへ送らない。fetch/XHR/ビーコンを一切書かないこと。
§14 の完了条件を1項目ずつ○×で報告して。
```

## 5. 年金事務所を探す(データ整備が主。最後で可)

```
docs/madoguchi-tool-design-2026-09-02.md と docs/site-mock-2026-09-02-tools/Madoguchi.html を読んで実装して。
モックは見た目とロジックの正。ただし窓口データはサンプルなので、§5 の手順で全国47都道府県分を作ること。ここが作業の大半になる。
予約の情報(§4)は機構の公式ページから再確認し、変わっていたら報告すること。
入力はサーバーへ送らない。§8の完了条件を1項目ずつ○×で報告して。
```

---

## 6. 5つ終わったら(まとめて)

```
/dougu の5つの道具が揃ったので、導線をつなげて。

1. /dougu の一覧を5枚すべて有効にする
2. 相互リンク: 目安→金額、金額→書類、書類→窓口・申立書、申立書→窓口
3. 記事側からの導線を入れる。挿入だけで、本文・URL・h1は変えない
   - /columns/moushitatesho-a4-insatsu の「先に結論」直後に申立書ツールのカード ★全クリックの47%を占める記事。慎重に
   - /columns/moushitatesho-kikan-kugiri に申立書ツール
   - /columns/teishutsusaki-yuusou に年金事務所ツール
   - /okane/ikura の冒頭に金額計算機
   - /hajimete に目安ツールと金額計算機
   - /shinsei の該当ステップに書類チェックリストと年金事務所
   - 精神系の病名ハブ(うつ・双極/適応・不安/統合失調症/発達/知的)に目安ツール
4. トップページに「道具」の帯を新設(4軸の分配盤の下)
5. グローバルナビにはまだ追加しない

終わったら、変更したファイルの一覧と、既存記事の本文に差分が出ていないことを報告して。
```

---

## 注意(全部に共通)

- **1つずつ渡す。**同時に複数やらせない。
- 各ツールは**別ブランチ**で。壊れたときに切り戻せるように。
- 完了条件の報告に×があったら、**次のツールに進む前に直す。**
- 公開前に `docs/prelaunch-checklist-2026-09-02.md` の検査を回す。
