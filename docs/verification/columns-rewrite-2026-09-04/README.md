# コラム47記事の取り込み・表示改修

作業ブランチ: `codex/columns-rewrite`（基準 `76deb22`）。元の作業ディレクトリと分離したworktreeで実装。pushはしていない。

## 実装

- `scripts/import-columns.mjs` で47本のlead・本文・日付・FAQを生成。変更コメントだけを除去し、本文の言い換えはしない。`--check` で再生成との一致を検査。
- 共通表示に結論、モバイルで閉じる目次、道具・誤解のリンク、裁決のPDFリンク、段落見出し、横スクロール表、最終確認日を追加。
- `jibun-de-shinsei` の本文をcontentへ移設。`hatachi-mae` と `ikura-moraeru` のrawContent + apply2026Amountsを維持。
- 裁決PDFの表示をCaseLeadとして既存の誤解カードと共通化。
- 出典は既存の資料URLに対応づけ。追加4URLは `curl -I -L` でHTTP 200を確認。未対応資料は原稿のままリンクなし。

## 検証

実行方法: `npm run build` → `npm run start -- --port 3107` → `node scripts/verify-columns.mjs http://localhost:3107`。

10項目と★3記事の結果は `results.json`。スクリーンショットは同じディレクトリ。

最終実行: build成功。①○ ②○ ③○ ④○ ⑤○ ⑥× ⑦○ ⑧○ ⑨○ ⑩○。
375pxは47ページで確認。デスクトップ本文幅は約700px。
原稿47本は元ディレクトリとのdiffゼロ。URL・slug・h1・title・metaTitleは作業前と一致。

公開前チェックは `npm run prelaunch:check -- http://localhost:3107 columns-rewrite-2026-09-04.md`。
ページ数166、A全項目○、C-1○。B-1/B-3/B-10は既存の別ページに関する×（/appなど）。詳細は `../prelaunch-2026-09-02/columns-rewrite-2026-09-04.md`。

## 原稿または検査方針の確認が必要

1. 10項目の⑥: `shoubyou-teatekin` の日額計算例6種類（13出現）が既存の `amounts-derive` では導出できない。1,667円、1,765円、4,020円、4,902円、5,000円、6,667円。金額の誤りという判定ではなく、既存検算関数の対象と原稿の計算例が一致しない。検査対象を黙って10万円以上に狭めたり、例外で合格にはしない。
2. ★ `teishutsusaki-yuusou`: Markdownの太字・リンク記法と空白を除く本文文字数は8,155→9,022、+10.63%。指定の±10%を超える。h2順序・既存リンク・アプリCTAは維持。完成原稿を勝手に短縮しない。

これらの解消前に「すべて合格」としてコミットしない。原稿変更や検査条件変更の了承待ち。

## 2026-09-04 仕上げ指示による検査条件の修正（追記）

上記は仕上げ前の判定として残す。`docs/codex-columns-rewrite-shiage-2026-09-04-instructions.md` の承認に従い、原稿を変えずに検査条件2件だけを修正した。

### ⑥ 金額

仕上げ指示§1に従い10万円以上を検算する。従来の「円」付きだけでなく、式の中の円を省略した数も抽出する。制度額の検算は既存 `amounts-derive` のまま。仮定値は `shoubyou-teatekin` に限定して宣言し、毎回理由と利用状況を出力する。使われなくなった宣言には警告を出す。

- 仮定値: 300,000(shoubyou-teatekin: 標準報酬月額の平均(「30万円なら」))
- 仮定値: 600,000(shoubyou-teatekin: 報酬比例部分(「報酬比例60万円」))
- 仮定値: 1,800,000(shoubyou-teatekin: 年金の合計(「年180万円なら」))
- 仮定値: 1,447,300(shoubyou-teatekin: 600,000 + basicGrade2(847,300))

人間側で検算済みの式（指示書§1-4をそのまま転記）:

```text
300,000 ÷ 30 × 2/3 = 6,666.7 → 6,667      6,667 × 30 = 200,010 → 約20万円
1,800,000 ÷ 360 = 5,000                   6,667 − 5,000 = 1,667(× 30 = 約5万円)
635,500 ÷ 360 = 1,765.3 → 1,765           6,667 − 1,765 = 4,902
600,000 + 847,300 = 1,447,300 → 約144.7万円    1,447,300 ÷ 360 = 4,020.3 → 4,020
4,020 × 365 = 1,467,300 → 約147万円        6,667 × 360 = 2,400,120 → 約240万円
```

### ★3本の文字数

結論の箱は47本共通のデザイン要素で、記事の厚みではないため、判定対象はfrontmatterのleadと変更コメントを除いた本文とする。±10%は変更しない。指示書の数え方に合わせ、Markdown記法は残して空白だけを除く。

なお、旧検査もleadは除いていた。旧判定との数値差は、旧検査が太字・リンク記法も取り除いていたことによる。leadを再度引いて合格にするのではなく、指示書で示された同一の数え方を新旧双方に適用した。

| 記事 | 旧本文 | 新本文 | lead | 本文+lead | 本文増減 | lead込み増減 |
|---|---:|---:|---:|---:|---:|---:|
| teishutsusaki-yuusou | 8,924 | 9,536 | 393 | 9,929 | +6.86% | +11.26% |
| moushitatesho-kikan-kugiri | 10,979 | 11,672 | 357 | 12,029 | +6.31% | +9.56% |
| moushitatesho-a4-insatsu | 11,054 | 10,813 | 295 | 11,108 | −2.18% | +0.49% |

h2順序・既存リンク集合・App CTAの検査条件は変更していない。

### 仕上げ後の検証結果

- 原稿47本: 元の作業ディレクトリとバイト一致、SHA-256一致（`manuscript-sha256.json`）。原稿の編集なし。
- クリーンビルド: 成功。環境が `rm -rf` を拒否したため `.next` を一時ディレクトリへ退避し、新しい `.next` を生成。サイトマップ166ページ維持。
- `npm run verify:columns`: ①○ ②○ ③○ ④○ ⑤○ ⑥○ ⑦○ ⑧○ ⑨○ ⑩○、終了コード0。
- 未説明額0件。仮定値4件すべて利用あり、未使用宣言0件。
- ★3本: すべて○。本文の増減は−2.18% / +6.31% / +6.86%。
- `npm run prelaunch:check`: 終了コード0、A全項目○、A-8○、B-2○、C-1○。`/jitsurei` は99本、宣言済みハブとして件数と理由を付記済み。
- B-1/B-3/B-10の既存指摘は変更せず報告に残す。
- 仕上げ後の公開前チェック: `../prelaunch-2026-09-02/columns-rewrite-shiage-2026-09-04.md`。
- 仕上げ前の判定: このREADMEの旧記録および `results-before-shiage.json` に保存。
