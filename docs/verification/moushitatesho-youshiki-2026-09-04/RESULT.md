# 検証結果: 申立書を公式様式の記入にする (2026-09-04)

設計 `docs/moushitatesho-youshiki-saigen-2026-09-04-design.md`、
仕上げ指示 `docs/codex-moushitatesho-youshiki-shiage-2026-09-04-instructions.md`。
ブランチ `codex/moushitatesho-youshiki`。

## §10 完了条件

| # | 判定 | 中身 |
|---|---|---|
| 1 | ○ | `docs/forms/moushitatesho/` に 01〜05。README に md5 と取得日(全5件) |
| 2 | ○ | 背景は SVG 4枚・**合計 1.83MB**、PNG 4枚は削除。検査0がサイズと整形式を毎回見る |
| 3 | ○ | `layout.ts` は §4 の値そのまま。自動縮小のコードは無い。続紙は 03.pdf から実測(`measure-cont.md`) |
| 4 | △ | 5項目のうち **1・2・3・5 と中心のずれは○**。**「楕円が字を丸ごと含む」だけ×**(下記)。欄ごとの表は `fields.md`、楕円の一覧は `circles.md` |
| 5 | ○ | `minimal` の表面で、発病日と初診日が同じ行の左右、令和に○、年月日が空欄の中央。§1 の症状は消えた |
| 6 | ○ | `typical` の裏面で職種〜その他・ア〜オ・1〜4・手帳①・請求者・申立日が入る。入力UIも足した(指示書 §1) |
| 7 | ○ | `seven` は続紙1枚(表5行)、通番 6〜7、No.2、枚中2、続紙の裏に請求者欄 |
| 8 | ○ | `max` はどの欄も縮小されず、入力画面に「収まりません(あと◯行ぶん)」が出て、印刷は止まらない |
| 9 | ○ | `fontPt: 9` で全 text スロットが 9pt になり、容量の表示が変わる |
| 10 | ○ | v1 の localStorage / JSON を読むと v2 に移行。`techouInfo` → `techouList[0]`。テスト6本 |
| 11 | × | 現行 §14 の全項目は未実施(下記) |
| 12 | ○ | 判定・評価・攻略・「様式第120号の4」は画面にも紙にも無い |

## §9-2 の検査(`npm run verify:moushitatesho:layout`)

```
○ 0. 背景SVG 4枚が Chrome の XML パーサを通り、合計 2MB 未満   4枚とも整形式 / 合計 1.83MB
○ 1. 文字がスロットの内側に収まっている(max を除く4サンプル)   144個
○ 2. 文字が公式様式の印字と重ならない                          313個
○ 3. digits の中心が、印字の間の空欄の中央から ±1.0mm          142個
○ 4. circle の中心が囲む文字の中心から ±0.5mm                  244個(うち133個は実文字と照合)
○ 5. 期間の状況の本文が枠の右端を超えない                      本紙279.1 / 続紙188.87
× 6. 楕円が囲む文字を丸ごと含む                                153/244件で楕円が小さい
```

サンプルは `empty` `minimal` `typical` `seven` `max` の5つ(`scripts/verify-moushitatesho/samples.mjs`)。

### 検査の作り方を設計から変えたところ(理由つき)

設計 §9-2 は「印字の矩形を PDF のオブジェクト一覧から採る」だったが、**01.pdf は
クリップされて描画されない図形を content stream に持っている**。年・月・日の空欄の中にも
黒い塗り図形(35点・23点の閉パス)の記述があり、オブジェクト一覧で判定すると
「空欄に数字を置いたら印字と衝突」という偽の結果になる。実際に描画すると空欄は白紙。

そこで検査2は **公式PDFを300dpiで描画した画素**を正にした(`scripts/verify-moushitatesho/ink.py`)。
人が紙で見るものと同じ。検査4の中心も、実文字がある欄(元号・年月日・続紙の全欄)は
実文字の矩形を、無い欄(01.pdf 裏面のアウトライン見出し)は楕円の内側のインクを使っている。

`official-glyphs.json` は記録として残してあるが、上の理由で衝突判定には使っていない。

## 検査6が × の理由(判断が要る)

長方形の字を楕円で**丸ごと**囲むには `rx ≧ 半幅×√2`、`ry ≧ 半高×√2` が要る。
設計 §4 の半径は、字の半幅・半高よりは大きいが √2 倍には足りない。

| 例 | 字の大きさ | §4 の rx / ry | 丸ごと含むのに必要 |
|---|---|---|---|
| 本紙 期間行「受診していない」 | 28.6 × 3.7mm | 15.5 / 2.8 | 20.24 / 2.63 |
| 本紙 発病日「平成」 | 9.15 × 4.94mm | 5.2 / 3.0 | 6.47 / 3.49 |

つまり §4 の楕円は、字の**中央の帯は覆うが四隅が少しはみ出す**。手で○を書いたときと同じ見え方で、
中心のずれは全件 ±0.5mm 以内(検査4は○)。**設計 §4 の数値と §9-2 の「完全に含む」が両立しない。**
数値を勝手に変えないため、× のまま報告する。

## §9-4 エクセル版との照合

**未実施。**この環境に LibreOffice が無く、`soffice --headless --convert-to pdf` を実行できない。
§10 の完了条件には入っていない項目。

## 環境メモ

- **poppler が入らない**(brew が2回とも失敗)。`pdftocairo` の代わりに PyMuPDF で SVG 化した
  (`scripts/forms-to-svg.py`)。座標を小数1桁に丸め、同じ形のパスを `<defs>`+`<use>` にまとめ、
  使わない属性を落として 2.68MB → **1.83MB**。公式PDFと画素で比べて、差は輪郭のアンチエイリアスだけ。
  途中で `inkscape` 名前空間の宣言だけ消して属性が残り、Chrome が XML エラーで真っ白になった。
  再発防止に、検査0が毎回 Chrome の `DOMParser` で整形式を確かめる。
- `scripts/measure-form.py` は **pdfplumber(/usr/bin/python3)でも PyMuPDF でも同じ値**を出す。
  罫線は両ファイルとも「細い塗り矩形」で描かれているので、短辺 0.8mm 未満を線として扱う。
- pdfplumber はこの環境の anaconda python3.8 には入らなかった(pip が壊れている)。
  `/usr/bin/python3`(Xcode 同梱)に入れてある。

## 残っている作業

- §10-11(現行設計 §14 の全項目)。旧 `scripts/verify-moushitatesho/verify.mjs` は v1 のデータと
  旧クラス名(`.mt-paper-text`)・自動縮小前提なので、そのままでは動かない。作り直しが要る。
- 設計 §9-3(記入済みで公式PDFに 50% 重ねた画像)
- 設計 §11(ページの文章と JSON-LD)
