# 「申立書を、自分で書きたい」作り直し 検証記録 (2026-09-03)

1. ○ 入口が「読み込んでいます…」なしで、サーバー描画のHTMLに h1 とリードと「はじめる」が含まれる
2. ○ 「はじめる」の前に、チェック・書き出し・読み込み・削除が無い
3. ○ 保存済みの下書きがあるとき、入口に「続きから／新しく始める」が出て、「続きから」で内容が復元される(既存の保存形式のまま)
4. ○ 「本来請求」「事後重症」「遡及」「裏面」「認定日頃」が画面のテキストに出ない
5. ○ 期間を区切る画面が、入口から数えて3画面目
6. ○ 画面5 の3択が内部の seikyuuType に正しく対応し、印刷で出る面が変わらない(既存の verify-moushitatesho #6 続紙と印刷の検査が通る)
7. ○ はみ出し検知の文言が出る。8pt 下限と警告(既存 #8)が通る
8. ○ 印刷の重ね合わせ(既存 #4)が通る。`MoushitateshoPrint.tsx` と `layout.ts` に差分0
9. ○ localStorage 無効でも入口→印刷まで進める(既存 #10)
10. ○ 375px で横スクロールなし。キーボードで最後まで(既存 #12)
11. ○ 回答内容を含む送信0件
12. ○ 記事側の3カードがバイト一致

## 実測

- サーバーHTML: h1・リード・「はじめる」あり、「読み込んでいます…」なし、入口内チェック0、ボタンは「はじめる」1つ
- 旧下書き: `hatsubyou=2020-01-01` を既存キーから読み、画面1に `2020-01` として復元
- 画面順: 入口 → 「まず、2つだけ教えてください。だいたいの月で構いません。」 → 「発病から今までを、いくつかの期間に分けます」
- 3択: 書く=`sokyuu` / 書かない=`jigojuushou` / あとで決める=`null`
- 印刷射影: 最大差 0.169mm
- 続紙: 期間1〜12すべて一致
- はみ出し: 現実的3期間は縮小0・警告0、最大入力は検出数・赤枠・画面件数一致、最小8pt
- localStorage無効: 「この端末では保存できません。閉じると消えます」を表示し、印刷画面到達
- JSON往復: 5/5 deepEqual
- キーボード: focus消失0、印刷画面到達
- 375px: `scrollWidth=375` / `clientWidth=375`
- 回答後のGET以外の通信: 0件
- 記事カード:
  - `moushitatesho-a4-insatsu`: 292バイト、本番と一致
  - `moushitatesho-kikan-kugiri`: 298バイト、本番と一致
  - `moushitatesho-kakikata`: 223バイト、本番と一致
- `components/tools/MoushitateshoPrint.tsx`: SHA-256 `9da517e9800721b81b4e9d9781044f764f65de1656f31f3df2632d660c05f282`
- `data/moushitatesho/layout.ts`: SHA-256 `9b8520e4417753b0b59650d93e68064923909c5ef703af7d29300a1c7d09b4a2`

## 公開前チェック

- `rm -rf .next && npm run build`: ○
- `npm run prelaunch:check`: ○
- ページ数: 166
- A: 全項目○
- C-1: ○
- 既存のB警告: B-1 `/app`、B-3 2件、B-10 3件
