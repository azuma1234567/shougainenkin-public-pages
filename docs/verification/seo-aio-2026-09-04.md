# SEO/AIO 作業の記録 (2026-09-04)

監査 `docs/seo-aio-audit-2026-09-04.md`、指示書 `docs/claude-code-seo-aio-2026-09-04-instructions.md`。

## Phase A: 47本の書き直しを本番に出す

`codex/columns-rewrite` は `2dbcfac` で main にマージ済み、`origin/main` にも反映済みだった
(申立書ツールを出したときに一緒に push されていた)。今回やったのは残りの docs のコミットと push。

- push: `7ac0ad6 docs: SEO/AIO 監査と指示書を追加`(`3b11de1..7ac0ad6`)
- `npm run typecheck` / `npm run build` / `node scripts/prelaunch-check.mjs` … ページ数166・A はすべて ○
- デプロイ確認(curl):

| URL | status | 更新日 2026年9月3日 | /dougu/ | /gokai | /jitsurei |
|---|---|---|---|---|---|
| /columns/shinsei-kikan | 200 | あり | 2種 | 2種 | あり |
| /columns/hitorigurashi-furi | 200 | あり | 2種 | 3種 | あり |
| /columns/moushitatesho-a4-insatsu | 200 | あり | 2種 | ハブ `/gokai` のみ | あり |

`moushitatesho-a4-insatsu` だけ `/gokai/<slug>` への深いリンクは無く、ハブ `/gokai` へのリンク1本。

## Phase B: T1〜T6

ブランチ `codex/seo-aio-2026-09-04`。**merge も push もしていない**(理由は下の「止まったところ」)。

| T | commit | 変えたファイル | 検証 |
|---|---|---|---|
| T1 | `332a166` | `lib/sitemap-static-dates.ts`(新規) / `app/sitemap.ts` / `lib/hub-content.ts` / `data/hubs/*.json` 44本 / `scripts/prelaunch-check.mjs` | `<url>` 166 = `<lastmod>` 166、`changefreq` 0・`priority` 0。C-6 を追加 |
| T2 | `37a284a` | `lib/hub-content.ts`(`extractHubFaqs`) / `components/platform/HubLanding.tsx` / `components/MarkdownArticle.tsx` / `scripts/verify-hub-content.mjs` | 40ハブに FAQPage 計123問。抽出と画面の質問が全件一致 |
| T3 | `ba517ba` | `components/platform/HubLanding.tsx` | h1 の下に `最終更新日 2026年9月2日` |
| T4 | `77509f9` | `public/5cb0e64cda83b784a5909863f71d1874.txt` / `scripts/indexnow-submit.mjs` / `package.json` | 鍵が 200 で返る。`--dry-run` で166件 |
| T5 | `ab7b77d` | `lib/columns.ts` / `content/columns/shikyuu-teishi-fukkatsu.ts` / `data/hubs/nayami-shikyuu-teishi.json` | **verify:columns の 1・8・9 が落ちる(下記)** |
| T6 | `ff07659` | `app/llms.txt/route.ts`(新規) | `/llms.txt` が 200・168行 |
| — | `2c47d43` | `scripts/prelaunch-check.mjs` / `data/hubs/nayami-shikyuu-teishi.json` | C-6 の誤検知を修正 |

### 途中で見つけて直したもの

- **T2 の副産物**: ハブの FAQ には「答えを次の行に書く形」と「同じ行に続けて書く形」があり、
  40本中17本が後者。後者は答えが `<summary>` に飲み込まれて本文が空だった。
  `MarkdownArticle` と `extractHubFaqs` の両方で切り分けるようにした。
- **パンくずの二重**: 監査 §4-2 は「HubLanding に breadcrumbJsonLd を出す」としているが、
  `<Breadcrumb>` が既に BreadcrumbList を出していた。足すと二重になるので出していない(B-9 ○)。
- **C-6 の誤検知**: 自分で足した `dateModified` 欄のせいで44本すべてが「古い」になった。
  「その行だけ直したコミット」を飛ばすようにした。

### 監査の表と実際の食い違い(直さずに合わせたもの)

- §4-3 の `hitsuyou-shorui` / `shoshinbi-fumei` はその slug の記事が無い。
  中身から `hitsuyou-shorui-seishin` / `shoshinbi-wakaranai` と判断した。
- §4-3 の `jushinjokyo-shomeisho` と `nenkin-jimusho-soudan` は**すでに満たしていた**ので触っていない。
- §4-2 の検証対象 `/erabu/hiyou-souba` には Q&A が無い。見えない FAQ は入れないので FAQPage も出さない
  (Q&A の無いハブは4本とも同じ)。

## 止まったところ(指示書 Phase B の「止まる条件: check が通らない」)

**T5 で `npm run verify:columns` の 1・8・9 が落ちる。T5 を入れる前は 10/10 ○ だった。**

| # | 検査 | 落ちる理由 |
|---|---|---|
| 1 | 47記事すべてに lead 3〜5項目・**原稿との完全一致** | `shikyuu-teishi-fukkatsu` のリード1文目に「打ち切り」を足したため(監査 §6-2 の指示) |
| 8 | URL・slug・h1・**title・metaTitle**が作業前と一致 | `shinsei-kikan` と `shoshinbi-wakaranai` に metaTitle を足したため(監査 §4-3 の指示) |
| 9 | 47記事の FAQPage と画面の Q/A が本文に一致 | `shikyuu-teishi-fukkatsu` に FAQ を1問足したため(監査 §6-2 の指示) |

どれも「47本を**書き直し前の baseline に固定する**」ためのガードで、監査が変えてよいと言っている
項目そのものを守っている。`docs/verification/columns-rewrite-2026-09-04/baseline.json`(baseRef `76deb22`)。

**h1 は変えていない。**このサイトは h1 が `lib/columns.ts` の `title` から出るので、
監査の「`<title>` を変える / h1 は変えない」を両立させるには `metaTitle` を使うしかない。
最初 `title` を直してしまい、h1 まで変わっていたので `metaTitle` に直した。

### 判断が要ること(2つのうちどちらか)

- **(a) baseline を今回の意図した変更ぶんだけ更新する。**
  `baseline.json` の該当2 slug の `metaTitle` / `htmlTitle` と、
  `shikyuu-teishi-fukkatsu` の lead・FAQ の照合元を書き換える。ガードは残るが、
  「作業前」の意味が「コラム書き直し前」から「SEO 作業前」に移る。
- **(b) T5 を取り消す。**監査 §4-3・§6-2 は、baseline の扱いを決めてから別立てでやる。
  T1〜T4・T6 だけを先に出す。

決まるまで merge も push もしない。

## 未実施

- `npm run indexnow`(デプロイ後にやる)
- Phase C(Bing)・Phase D(URL 検査)はブラウザ作業。2本目のコマンド待ち

## そのほか

- `npm run verify:hubs` の `verify-hub-map.mjs` は**作業前の main でも exit=1**(`/jitsurei` の被内部リンク100本)。
  今回の変更とは関係がないので触っていない。
- `prelaunch-check` の × は B-1・B-3・B-10 の3つで、作業前と同じ顔ぶれ。
