# 申請の流れ(StepFlow)を左から右へ 検証記録(2026-09-05)

指示書: `docs/stepflow-yokonarabi-2026-09-05-instructions.md`
優先: `user-psychology-seisa §4` > `page-types-seisa §4` > `design-seisa` > 本指示書。
見た目の正は `docs/site-mock-2026-09-05-all/site.html`。色・文字・角丸・影は
design-system のトークンだけを使い、指示書の hex・px はトークンに読み替えた。

対象は `components/platform/StepFlow.tsx` と `app/platform.css` の `.step-flow*` の1か所。
トップと /shinsei のヒーローで同じ部品を使う。

## 実装の状態

§2・§3 のうち、次は 4cb0614(2026-09-05)で入っている:

- 1行に 1→8。番号の円を線でつなぎ、左から右へ。番号の下にステップ名(PCは2行)。
- 道具カードはそのステップの真下(3・4・5・6 の4枚。6 は塗りつぶし)。
- 幅の切り替え: 1181px〜 1行×8列 / 701〜1180px 4列×2行(4の右の線は消す・行間36px) /
  〜700px 縦一列(番号の下に縦線、道具カードは番号の右)。
- `<ol class="step-flow">` + `<li class="step-flow-node" data-n="1..8">`。
  当て方は `data-n` で、`nth-of-type` は使っていない。
- ステップ名は `/shinsei#step-N` へのリンク。カードの文言は `data/dougu.ts` の
  `shinseiSteps` のまま。カードの見出しは `--fs-small`、説明は `--fs-meta`。
- リードは「初診日の確認から結果が届くまで。左から右へ、順番に進みます。」(トップ)。

今回足したのは1つだけ:

- **道具のあるステップの円に輪**を出した。指示書は
  `box-shadow: 0 0 0 4px #fff, 0 0 0 6px #e8f4fc` だが、
  (1) 新しい hex を書かない (2) 影は1種類だけ、の2つを守るため
  `outline: 2px solid var(--c-border); outline-offset: 4px` で描いた。
  棚卸しは 影1種・色10種のままで基準内。

## 入れなかったもの — 上の4つの区切り(§2・§3の PHASES)

指示書は「確かめる / そろえる / 書く / 出す・待つ」の4区切りを求めているが、
**2026-09-05 に利用者から「このデザインは消す」と指示があり、4cb0614 で外している**。
本書より新しい直接の指示なので、今回も入れていない(`.step-flow-phase` は0個)。
入れ直す場合は言ってください。CSS ごと戻す形になる。

なお `user-psychology-seisa §4` と `page-types-seisa §4` は、どちらも stepflow は
「変更なし」としている。

## §5 の検証

**1. typecheck / build → ○** どちらもエラーなし。

**2. スクリーンショット → ○**
`docs/verification/stepflow-2026-09-05/` に、トップと /shinsei を 1500 / 1000 / 390px で
6枚(`top-1500.png` など)。モックと並べて、番号の並び・線・カードの位置が同じ形。

**3. 1500px で 1〜8 の円の y が同じ → ○(ぶれ 0px)**

| ページ | 円の数 | y | ばらつき |
|---|---:|---|---:|
| / | 8 | 727 のみ | 0px |
| /shinsei | 8 | 366 のみ | 0px |

**4. 1000px と 390px → ○**

| 幅 | 形 | 実測 |
|---|---|---|
| 1000px | 4列×2行 | y は2種類(1〜4 と 5〜8)・x は4種類 |
| 390px | 縦一列 | x は1種類・y は8種類 |

**5. Tab の順番 → ○** DOM 順のまま 1→8。道具のあるステップでは、ステップ名 → 道具の順。

```
1:ステップ名 → 2:ステップ名 → 3:ステップ名 → 3:道具 → 4:ステップ名 → 4:道具
→ 5:ステップ名 → 5:道具 → 6:ステップ名 → 6:道具 → 7:ステップ名 → 8:ステップ名
```

**6. 公開前チェック → ○** ページ数 166、× は **B-1・B-3** の2つ。
B-10 は 2026-09-05 の追補で解消済みなので、基準(B-1・B-3・B-10)から増えていない。

## 変えていないもの(§4)

- `STEPS` の8つの名前と順番。
- `data/dougu.ts`(道具の文言・配置)。
- `/shinsei` の本文(`shinsei-steps` 以下)とヒーローの導入文。
- HowTo の JSON-LD。
