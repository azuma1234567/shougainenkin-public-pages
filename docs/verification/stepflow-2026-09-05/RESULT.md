# 申請の流れ 8ステップ 横並び化 検証記録(2026-09-05)

指示書: `docs/stepflow-yokonarabi-2026-09-05-instructions.md`
モック: `docs/site-mock-2026-09-05-stepflow/stepflow-mock.html`
対象: `components/platform/StepFlow.tsx` / `app/platform.css` の `.step-flow*`
使われている場所: トップ(`app/page.tsx`)と `/shinsei` のヒーロー。両方で同じ部品なので、直したのは1か所。

## 1. typecheck / build

```
npm run typecheck  → エラーなし
npm run build      → ✓ Compiled successfully / 271ページを生成
```

## 2. スクリーンショット(モックと並べて確認)

`/` と `/shinsei` を 1500 / 1000 / 390px で撮り、同じ幅のモックも撮って並べた。

| 幅 | 実装 | モック | 形 |
|---|---|---|---|
| 1500px | `top-1500.png` / `shinsei-1500.png` | `mock-1500.png` | 1行×8列。上に4つの区切り。線は左から右へ。カードは 3・4・5・6 の真下。6 は塗りつぶし |
| 1000px | `top-1000.png` / `shinsei-1000.png` | `mock-1000.png` | 4列×2行(1〜4 / 5〜8)。区切りなし。4 の右の線は消えている |
| 390px | `top-390.png` / `shinsei-390.png` | `mock-390.png` | 縦一列。番号の下に縦線、カードは番号の右の列。ステップ名は1行 |

番号の並び・線・カードの位置は、3つの幅ともモックと同じ形になっている。
モックのほうが1枚あたり横に広いのは、モックの `.wrap` が最大1660pxで、
本番の `p-container` より広いため。レイアウトの形は同じ。

撮影時はアクセス解析の同意バーが道具カードに重なるので、先に「拒否する」で閉じている。

## 3. 1500px で 1〜8 の円の y 座標(横一直線の証明)

`page.locator('.step-flow-node .step-flow-number')` の boundingBox。

| ページ | y 座標(1〜8) | 判定 |
|---|---|---|
| `top-1500` | 724.8 / 724.8 / 724.8 / 724.8 / 724.8 / 724.8 / 724.8 / 724.8 | **ばらつき 0.0px** |
| `shinsei-1500` | 380.8 / 380.8 / 380.8 / 380.8 / 380.8 / 380.8 / 380.8 / 380.8 | **ばらつき 0.0px** |

**8個とも完全に同じ y。**指示の ±1px より厳しく、ばらつき 0.0px。

## 4. 1000px と 390px

| ページ | 1行目 | 2行目 |
|---|---|---|
| `top-1000` | 1〜4: 508.1(ばらつき 0.0px) | 5〜8: 765(ばらつき 0.0px) |
| `shinsei-1000` | 1〜4: 309.9(ばらつき 0.0px) | 5〜8: 588.5(ばらつき 0.0px) |

| ページ | x 座標 | 判定 |
|---|---|---|
| `top-390` | x = 20 が8個 | **全部同じ** |
| `shinsei-390` | x = 20 が8個 | **全部同じ** |

## 5. Tab の順(DOM 順の確認)

1500px のトップで、最初のステップ名にフォーカスしてから Tab を押した順。

| # | ステップ | 種類 | 文字 |
|---|---|---|---|
| 1 | 1 | ステップ名 | 初診日を確認する |
| 2 | 2 | ステップ名 | 納付要件を確認する |
| 3 | 3 | ステップ名 | 年金事務所へ相談する |
| 4 | 3 | 道具 | → どこに出せばいい？管轄の年金事務 |
| 5 | 4 | ステップ名 | 必要書類をそろえる |
| 6 | 4 | 道具 | → 何をそろえればいい？自分の場合に |
| 7 | 5 | ステップ名 | 診断書の準備をする |
| 8 | 5 | 道具 | → 私は何級くらい？受け取った診断書 |
| 9 | 6 | ステップ名 | 申立書を作成する |
| 10 | 6 | 道具 | → 申立書を、自分で書きたい期間ごと |
| 11 | 7 | ステップ名 | 年金事務所へ提出する |
| 12 | 8 | ステップ名 | 結果を待つ |

**1→8 の順**に、ステップ名 → (あれば)その段の道具カード、と止まる。
DOM は区切り4つ → ステップ1〜8 の順で、区切りは `aria-hidden` かつリンクを持たないので Tab では止まらない。

## 6. 公開前チェック

```
node scripts/prelaunch-check.mjs http://localhost:3200
ページ数: 166
× の項目: B-1 / B-3 / B-10
```

直前の main と同じ **B-1・B-3・B-10 の3つだけ**で、増えていない。

## 実装のメモ

- ルートは `<ol class="step-flow">`、各ステップは `<li class="step-flow-node" data-n="1..8">`、
  区切りは `<li class="step-flow-phase" aria-hidden="true">`。
  区切りも `<ol>` の子なので `nth-of-type` はずれる。線の制御は `[data-n="4"]` `[data-n="8"]` で当てている。
- ステップ名は `STEPS` の文字列のまま。PC で2行に割るために `{title, split}` の形にして、
  `split` の位置で `<span class="step-flow-break">` に分けた(文字列に `<br>` は入れていない)。
  スマホでは `.step-flow-break` を `display: inline` に戻して1行にする。
- ステップ名は `/shinsei#step-N` へのリンク、道具カードは `placementCard(placement).href`。
  カードの文言は `data/dougu.ts` の `shinseiSteps` をそのまま使っている(変えていない)。
- 区切りの文言は `StepFlow.tsx` の定数 `PHASES = ["確かめる","そろえる","書く","出す・待つ"]`。

## 変えていないもの(§4)

- `STEPS` の8つの名前と順番。
- `data/dougu.ts`(道具の文言・配置)。
- `/shinsei` の本文(`shinsei-steps` 以下)。ヒーローの `StepFlow` だけが変わった。
- HowTo の JSON-LD(`ShinseiRestyled.tsx` の独自 `STEPS` を使っており、この変更とは無関係)。

トップのリードだけ、指示書 §2 のとおり
「初診日の確認から結果が届くまで。左から右へ、順番に進みます。」に差し替えた。
