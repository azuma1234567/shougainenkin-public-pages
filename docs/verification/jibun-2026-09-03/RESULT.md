# 「自分の場合を確かめる」への作り直し 検証記録(2026-09-03)

指示書: `docs/codex-jibun-no-baai-2026-09-03-instructions.md` §1〜§4

## 1. 表示文字列に「道具」「ツール」「置き場所」が残っていないか

5つの機能まわりからは**すべて消した**。残っているのは**iPhoneアプリの説明**で、この5機能の話ではないので触っていない。

| 場所 | 文字列 | 触らなかった理由 |
|---|---|---|
| `app/support/page.tsx:70` | 本アプリは申請準備を補助する記録・整理**ツール**で… | アプリの説明 |
| `components/ApplicationFlowPage.tsx:1100,1107` | 申請準備の**道具** / 進めるための**道具**です | アプリの導線 |
| `components/platform/ShinseiRestyled.tsx:123` | 申請準備の**道具** / 進めるための**道具**です | アプリの導線 |
| `lib/app-legal.ts:88` | …整理するための補助**ツール**です | アプリの法務文面。勝手に書き換えない |

コメント2件(`components/platform/DouguCard.tsx:2`、`data/dougu.ts:3`)は指示書の対象外(コメント・識別子・docs)。
URL `/dougu` とコード内の `dougu` 識別子は変えていない。

`kakikata` の記事内カードの cta は「道具を開く」だったので、TOOLS の既定「申立書をつくる」に寄せた
(指示書に置き換え文言の指定が無く、表示文字列から「道具」を消す必要があったため)。

## 2. 公開前チェック

```
ページ数: 167(増減なし)
A-1〜A-10: すべて ○
C-1: ○
```

| | 前回(導線を足したとき) | 今回 | |
|---|---|---|---|
| B-1 孤立 | 1 / 167 | 1 / 167 | 悪化なし |
| B-2 被リンク50本超 | 0 | 0 | 悪化なし |

B-1 の孤立は `/app` で、今回の変更より前から続いているもの。

## 3. 既存記事に触っていないこと

- `/columns/moushitatesho-a4-insatsu` の `<aside class="mt-column-card">` が
  **ローカルと本番でバイト一致**(162バイト)。この記事には触っていない。
- `content/columns/` と `data/hubs/` の差分 **0ファイル**。

## 4. スクリーンショット

375px と 1280px で トップ・`/dougu`・`/dougu/mitate` を保存した。
いずれも `scrollWidth == clientWidth`、はみ出し要素 **0**。

| | 375px | 1280px |
|---|---|---|
| トップ | `top-375.png` | `top-1280.png` |
| /dougu | `dougu-375.png` | `dougu-1280.png` |
| /dougu/mitate | `mitate-375.png` | `mitate-1280.png` |

## 5. ナビの375px(指示書 §3-2)

**崩れていない。**375px ではデスクトップの横並びナビは非表示になり、ハンバーガーに入る。
開いたときの順番は「はじめての方へ → **自分の場合** → 病気から探す → 悩みから探す → 申請の流れ →
実例と数字 → コラム」で、指示どおり「はじめての方へ」の次。
`scrollWidth = clientWidth = 375`、はみ出し要素0(メニューを開いた状態でも0)。
