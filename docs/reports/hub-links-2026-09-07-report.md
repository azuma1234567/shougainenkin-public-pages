# ハブ本文の「ラベル(/path)」を箇条書き・番号つき・文中でもリンクにする(2026-09-07 指示書)結果報告

指示書: docs/hub-links-2026-09-07-instructions.md §1〜§3。直したのは `lib/hub-content.ts` の `prepareHubSource()` だけ。`data/hubs/*.json` は **diff 0 行**。push はしていない。作業日 2026-09-08。

コミット: `fix(hubs): 本文の「ラベル(/path)」を箇条書き・番号つき・文中でもリンクにする`

## 直し方(§1)

行ごとに次の順で変換する(`apply2026Amounts` のあと)。

1. 「→ 」のある行: 既存の規則(1本目 + 「、」「/」で続く2本目以降)。**ラベルに括弧を含む「→ 目の障害(糖尿病網膜症など)(/byoki/shikaku)」は既存の規則でも漏れていた**ので、最後の `(/path)` をリンクにするように広げた(下記の2本)。
2. 箇条書き `- TEXT(/path)` `* TEXT(/path)` と番号つき `N. TEXT(/path)` `N. TEXT(/path)(注記)`: 行末が `(/path)` か `(/path)(注記)` のとき TEXT 全体をラベルに(TEXT に `(…)` や `**` があってよい)。
3. それ以外は文中の規則: 直前の区切り(、 。 「 」 行頭 空白)から `(` までをラベルに。

path は「`(/` で始まり `)` で終わり、空白と `)` を含まない」。`](` を含む行(markdown 化済み)は 2・3 で触らない。番号つきの `[**等級の目安をしらべる**](/columns/…)` は `MarkdownArticle` がリンクの中の `**` も描くので、太字のリンクになる。

## §2 検証

| # | 内容 | 結果 |
|---|---|---|
| 1 | typecheck / build / prelaunch / site-graph / verify:hubs | ○ / ○ / × は **B-10 だけ**(C-6・C-7 ○)/ **10 項目 ○** / **○**(本文21ページ一致、FAQ 152 件一致。データ不変) |
| 2 | `data/hubs/*.json` の diff | **0 行** |
| 3 | 49本 + /jukyuugo の `<main>` の**テキストノード**に `(/columns/` `(/byoki/` … `(/yougo` | **1 件**(下記。リンクではない文字列)。`[[` を含むページ 0 |
| 4 | 変換したリンクが全部 200、`[[` 無し | ○ 変更前に無く変更後に本文に現れたリンク先 **32 種類すべて 200**(変換した本数は 160、うち相異なる path が 32)。`[[` 0 |
| 5 | 4ページで関連記事の箇条書きが青いリンク、`(/columns/…)` の文字が見えない | ○ /byoki/utsu-soukyoku 15・/joukyou/hatarakinagara 11・/nayami/fushikyu 11・/okane/ikura 14 の `li` に `/columns/` へのリンク(色 rgb(2,115,173) = primary)。画面の文字「(/columns/」0。1400px / 390px のスクリーンショット 8 枚を docs/verification/hub-links-2026-09-07/ に |
| 6 | 「→ 」の行の既存の見た目 | ○ /nayami/fushikyu の「→ 」の段落 5 本の outerHTML が変更前後で完全一致 |
| 7 | site-graph 検査2 の数字が増える方向にだけ動く | ○ ハブ50ページの本文リンク(ナビ・パンくず除く)合計 **345 → 505(+160)**、被リンクが**減った先 0**、増えた先 44 種類。site-graph 検査2 の未達は変更前と同じ 2 件(`/gokai/gennin-ga-aru` 1本・`/gokai/counseling` 0本。ハブ本文はこの2枚を指していない) |

生ログ: docs/verification/hub-links-2026-09-07/(check.mjs・checks.txt・converted.md・verify-hubs.txt・prelaunch.txt・site-graph.txt・png 8 枚)。

## 変換したリンク(160 本)

指示書の 158 本(箇条書き 153 / 番号つき 4 / 文中 1)に、既存の「→ 」規則で漏れていた 2 本(ラベルに括弧)を足して 160 本。ハブ / 行の種類 / path の一覧は **docs/verification/hub-links-2026-09-07/converted.md**(160 行)。

- 「→」行でラベルに括弧があり漏れていた 2 本: `/byoki/tounyou`「→ 目の障害(糖尿病網膜症など)(/byoki/shikaku)」、`/erabu/jibun-ka-irai`「年金事務所へ行くなら → 年金事務所に持っていくもの(チェックリスト)(/columns/nenkin-jimusho-soudan)」。
- 変換後の本文で、`[ラベル](/path)` の形は 49 本で **320 本**(変更前 160 + 今回 160)。`(/path)` の形で残るものは、ソースの段階で 0。

## 指示書と違う点・残ったもの

1. **#3 で 1 件残る**: `/erabu/fushikyu-no-ato` の出典の行「社会保険審査会 裁決例(/jitsurei に原文リンク)」。`(/jitsurei に原文リンク)` は空白を含むので、§1 の path の定義(空白を含まない)ではリンクではなく、原稿の注記として文字のまま出ている。データは1文字も変えない約束なので触っていない。リンクにするなら原稿(data/hubs/erabu-fushikyu-no-ato.json)を「…裁決例(→ 実例(/jitsurei) に原文リンク)」のように書き換える必要がある。
2. 「→ 」規則の広げ方(ラベルに括弧を許す)は指示書に無いが、#3 を 0 に近づけるために足した(2本)。ほかの「→ 」行の出力は変わらない(検証6)。
3. 検証4 の「158本が全部 200」は、リンク先の種類(32)で確認した(同じ path が複数のハブから指されるため)。
